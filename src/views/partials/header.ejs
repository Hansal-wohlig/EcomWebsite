<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title><%= title %> | ByteCart Admin</title>
  <link rel="stylesheet" href="/css/styles.css" />
</head>
<body>
<header class="navbar">
  <div class="container">
    <a class="brand" href="/">⚙️ ByteCart</a>
    <% if (user && user.role === 'admin') { %>
      <nav>
        <a href="/admin/products">Products</a>
        <a href="/admin/categories">Categories</a>
        <a href="/logout" class="neon-btn danger">Logout</a>
      </nav>
    <% } %>
  </div>
</header>
